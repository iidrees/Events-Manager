language: node_js

node_js:
  - "8.2.1"

script:
  - npm test

install:
  - npm install

env:
  - NODE_ENV=test
services:
  - postgresql

before_script:
  - psql -c "create database events_manager_test;" -U postgres

after_success:
  - npm run coverage